import ComposeMailerClient from "./ComposeMailerClient";

export const metadata = { title: "Compose Mailer — RPM Prestige" };

export default function ComposeMailerPage() {
  return <ComposeMailerClient />;
}
